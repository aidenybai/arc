{
	"name": "spark",
	"version": "0.1.0",
	"description": "A tiny, web-standard, Hono-like web framework for Bun. Zero dependencies, zero build step.",
	"type": "module",
	"module": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"example": "bun run examples/hello.ts",
		"build:arc": "bun build arc/spark-on-arc.ts --outfile arc/spark-on-arc.js --target browser",
		"test:arc": "bun run build:arc && cd .. && gleam run -- framework/arc/spark-on-arc.js"
	},
	"devDependencies": {
		"@types/bun": "^1.3.0",
		"typescript": "^5.9.3"
	}
}
